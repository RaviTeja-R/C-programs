#include <stdio.h>
int main()
{
    int a[4],i,sum=0;
    for(i=0;i<4;i++)
    {
        scanf("%d",&a[i]);
    }
    for(i=0;i<4;i++)
    {
        if(a[i]>=10)
        sum=sum+1;
    }
    printf("%d",sum);
    return 0;
}
